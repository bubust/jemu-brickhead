# GitHub Pages 直接從 main branch 自動部署，無需額外 workflow
# 此檔案保留備用
name: No-op

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "GitHub Pages auto-deploys from main branch"
